---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres
  namespace: redmine
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-ceph-hdd-gz1-retain
